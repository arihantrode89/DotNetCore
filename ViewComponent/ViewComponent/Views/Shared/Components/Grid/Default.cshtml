@model IEnumerable<Person>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Gender</th>
        </tr>
    </thead>
    <tbody>
       @*  @foreach(Person item in ViewBag.person)
        {
            <tr>
                <th>@item.PersonName</th>
                <th>@item.Email</th>
                <th>@item.Gender</th>
            </tr>
        } *@

        @foreach (Person item in Model)
        {
            <tr>
                <th>@item.PersonName</th>
                <th>@item.Email</th>
                <th>@item.Gender</th>
            </tr>
        }
        
    </tbody>
</table>